<div dir="rtl">

# معرفی پروژه MQTT GUI

## ۱. هدف پروژه

MQTT GUI برای تبدیل‌شدن به یک **سرور مرکزیِ کنترل و مانیتورینگ دستگاه‌های متصل** طراحی شده است.

این پروژه می‌تواند روی یک کامپیوتر که همیشه در محل نصب روشن است اجرا شود؛ برای مثال:

- یک کامپیوتر کوچک Linux مثل Raspberry Pi یا Mini PC در خانه؛
- یک کامپیوتر صنعتی یا gateway محلی در ساختمان، کارگاه، مزرعه یا کارخانه؛
- یک سرور داخل شبکهٔ خصوصی.

گفتنِ «سیستم نهفته» برای این کامپیوتر مرکزی همیشه دقیق نیست. سنسورها و کنترلرهای جداگانه ممکن است سیستم‌های نهفته باشند، اما خود این پروژه بهتر است یک **سرور مرکزی یا gateway محلی** نامیده شود.

سرور مرکزی Mosquitto MQTT Broker و این پنل وب را اجرا می‌کند. سنسورها، بردهای ESP32، میکروکنترلرها و هر MQTT client دیگری به آن متصل می‌شوند. کاربرِ مجاز از یک پنل وب می‌تواند دستگاه‌ها را مانیتور کند، وضعیت اتصال آن‌ها را ببیند، تنظیمات Broker را مدیریت کند و در نسخه‌های بعدی دستگاه‌ها را کنترل یا داده‌های آن‌ها را بررسی کند.

هدف بلندمدت پروژه، ساخت یک پلتفرم IoT محلی و کاربردی است: دستگاه‌ها به‌جای داشتن سرور جداگانه، با یک نقطهٔ مرکزیِ قابل اعتماد ارتباط برقرار می‌کنند.

## ۲. امکانات نسخهٔ فعلی

نسخهٔ فعلی پایهٔ این پلتفرم را فراهم می‌کند:

- پنل وب با Flask.
- ثبت‌نام و ورود کاربر.
- ذخیرهٔ رمز عبور با bcrypt hash.
- محافظت از صفحه‌ها برای کاربران واردشده.
- پیکربندی Mosquitto Broker از طریق پنل وب.
- فیلدهای تنظیم MQTT، TLS و WebSocket.
- داشبورد دستگاه‌ها با نمایش آنلاین و آفلاین بودن.
- ذخیرهٔ آخرین وضعیت هر دستگاه در SQLite.
- ذخیرهٔ زمان آفلاین‌شدن دستگاه و نمایش آن به‌عنوان آخرین زمان آنلاین بودن.
- حذف یک دستگاه و سابقهٔ وضعیت ذخیره‌شدهٔ آن از پنل.
- اسکریپت نصب و حذف Linux برای ساخت سرویس‌های systemd.

## ۳. معماری سیستم

پروژه چهار بخش اصلی دارد:

```text
دستگاه‌ها و سنسورهای MQTT
          │
          │ پیام‌های MQTT
          ▼
Mosquitto Broker روی سرور مرکزی
          │
          │ اشتراک MQTT محلی
          ▼
برنامهٔ Flask و listener وضعیت دستگاه‌ها
          │
          │ دیتابیس SQLite و HTTP API
          ▼
پنل وب در مرورگر کاربر
```

### دستگاه‌های MQTT

دستگاه‌ها می‌توانند برد ESP32، سنسور، رله، کنتور، کنترلر یا هر MQTT client دیگری باشند. وظیفهٔ آن‌ها اتصال به Broker و ارسال وضعیت یا دادهٔ سنسورها است.

### Mosquitto Broker

Mosquitto نقش message broker را دارد. پیام‌های MQTT را از دستگاه‌ها دریافت می‌کند و برای subscriberها می‌فرستد. در این پروژه با سرویس systemd به نام `mosquitto-project.service` اجرا می‌شود.

### برنامهٔ وب Flask

برنامهٔ Flask با سرویس `mqtt-gui.service` اجرا می‌شود. صفحه‌های وب را نمایش می‌دهد، ورود کاربران را مدیریت می‌کند، تنظیمات Broker را ذخیره می‌کند و APIهای لازم برای داشبورد دستگاه‌ها را در اختیار پنل قرار می‌دهد.

### دیتابیس‌های SQLite

پروژه فعلاً از فایل‌های SQLite محلی استفاده می‌کند:

| دیتابیس | کاربرد |
| --- | --- |
| `login_signup.db` | ذخیرهٔ کاربران پنل وب و bcrypt hash رمزهای آن‌ها. |
| `device_status.db` | ذخیرهٔ نام دستگاه، وضعیت فعلی و زمان آخرین آنلاین بودن. |

## ۴. فایل‌های اصلی پروژه

| فایل یا پوشه | مسئولیت |
| --- | --- |
| `main.py` | برنامهٔ اصلی Flask، مسیرها، بررسی ورود، API تنظیمات، API دستگاه‌ها و API حذف دستگاه. |
| `connector.py` | ساخت یک MQTT listener برای برنامهٔ وب و دریافت پیام‌های وضعیت دستگاه‌ها. |
| `device_status.py` | توابع دیتابیس دستگاه‌ها: ذخیره، خواندن همه و حذف دستگاه. |
| `login_signup.py` | ساخت و اعتبارسنجی حساب‌های پنل وب. |
| `set_mosquitto.py` | ساخت `mosquitto.conf` از روی تنظیمات JSON ذخیره‌شده. |
| `json.json` | تنظیمات Broker انتخاب‌شده از پنل. |
| `templates/` | صفحه‌های HTML ورود، ثبت‌نام، داشبورد، تنظیمات Broker و دستگاه‌ها. |
| `install.sh` | نصب وابستگی‌ها، ساخت کاربر و سرویس‌ها و اجرای پروژه در Linux. |
| `uninstall.sh` | حذف سرویس‌ها و فایل‌های نصب‌شدهٔ پروژه. |
| `DEVICE_STATUS_GUIDE.md` | راهنمای کوتاه سمت دستگاه برای ارسال پیام آنلاین و آفلاین MQTT. |

## ۵. وضعیت آنلاین و آفلاین دستگاه‌ها

برنامهٔ فعلی به پیام‌های وضعیت دستگاه روی این MQTT topic گوش می‌دهد:

```text
isonline
```

هر دستگاه باید پیام JSON شامل نام یکتا و وضعیت بفرستد.

وقتی اتصال دستگاه با موفقیت برقرار شد، باید بلافاصله این پیام را منتشر کند:

```json
{"name": "esp32-room2", "status": "online"}
```

وقتی دستگاه در دسترس نیست، Broker باید این پیام را منتشر کند:

```json
{"name": "esp32-room2", "status": "offline"}
```

برنامه وضعیت فعلی را ذخیره می‌کند. وقتی پیام `offline` می‌رسد، زمان دقیق آن رویداد را هم ذخیره می‌کند. صفحهٔ Devices این زمان را به‌عنوان آخرین زمان آنلاین بودن دستگاه نمایش می‌دهد.

## ۶. MQTT Last Will and Testament

هر دستگاه باید قبل از اتصال، MQTT **Last Will and Testament (LWT)** را تنظیم کند.

Last Will باید از همان نام دستگاه استفاده کند و یک پیام JSON با وضعیت `offline` روی topic `isonline` بفرستد. اگر دستگاه بدون ارسال پیام قطع عادی، برق، Wi-Fi یا اتصال MQTT خود را از دست بدهد، Mosquitto به‌صورت خودکار Last Will را منتشر می‌کند. این کار باعث می‌شود پنل دستگاه را آفلاین نشان دهد.

ترتیب پیشنهادی اتصال:

1. Last Will را با پیام `offline` تنظیم کن.
2. به MQTT Broker وصل شو.
3. بلافاصله پس از اتصال موفق، پیام `online` را منتشر کن.

## ۷. محدودیت‌های فعلی

این پروژه در مرحلهٔ ابتدایی است و محدودیت‌های زیر را دارد:

- دیتابیس دستگاه‌ها آخرین وضعیت را ذخیره می‌کند، نه تاریخچهٔ کامل همهٔ تغییرات وضعیت.
- اگر دستگاهی را از پنل حذف کنی و بعداً دوباره پیام وضعیت MQTT بفرستد، دوباره در پنل نمایش داده می‌شود.
- topic مشترک فعلی برای توسعهٔ اولیه ساده است. برای سیستم بزرگ‌تر، بهتر است هر دستگاه topic خودش را داشته باشد؛ مثلاً `isonline/esp32-room2` و سرور به `isonline/#` گوش دهد.
- یک retained MQTT topic مشترک فقط می‌تواند یک retained message را هم‌زمان نگه دارد. برای retained state قابل اعتماد همهٔ دستگاه‌ها، topic جدا برای هر دستگاه لازم است.
- اجرای فعلی Flask ساده است. استقرار production در اینترنت عمومی به HTTPS، Firewall، reverse proxy و WSGI server مناسب نیاز دارد.
- درخواست‌های مرورگر که تنظیمات را تغییر می‌دهند یا دستگاه حذف می‌کنند، در آینده باید CSRF protection داشته باشند.

## ۸. نکات امنیتی

- رمزهای پنل وب به‌صورت bcrypt hash ذخیره می‌شوند، نه متن ساده.
- اسکریپت نصب برای سرویس محلی MQTT اطلاعات کاربری خصوصی می‌سازد.
- فایل‌های credential MQTT مخفی و خارج از Git هستند.
- اسکریپت نصب برای سرویس نصب‌شده یک Flask secret key تصادفی ایجاد می‌کند.
- پنل وب نباید بدون HTTPS و محافظت شبکه مستقیماً در اینترنت عمومی قرار بگیرد.
- وقتی دستگاه‌ها از شبکهٔ غیرقابل‌اعتماد یا اینترنت ارتباط می‌گیرند، TLS باید فعال باشد.

## ۹. برنامه‌های آینده

موارد زیر برنامهٔ نسخه‌های آینده هستند و **در نسخهٔ فعلی پیاده‌سازی نشده‌اند**.

### کتابخانه‌های C++ برای میکروکنترلرها

در آینده قرار است یک کتابخانه یا ماژول کوچک C++ برای میکروکنترلرها و دستگاه‌های نهفته اضافه شود تا اتصال دستگاه‌ها ساده‌تر شود:

- اتصال به Wi-Fi و MQTT با کد تکراری کمتر.
- تنظیم خودکار MQTT Last Will.
- ارسال خودکار پیام آنلاین بعد از اتصال.
- مدیریت reconnect و اعلام درست آفلاین‌شدن از طریق Last Will.
- توابع ساده برای ارسال دادهٔ سنسورها و دریافت فرمان‌ها.

این بخش باید اشتباهات هنگام راه‌اندازی را کم کند و اضافه‌کردن ESP32 یا هر دستگاه میکروکنترلری دیگر را به پلتفرم ساده‌تر کند.

### کنترل دستگاه و دادهٔ سنسورها

ماژول‌های بعدی می‌توانند فرمان برای رله، چراغ، موتور، آلارم و actuatorهای دیگر اضافه کنند. همچنین داده‌هایی مانند دما، رطوبت، مصرف انرژی، سطح آب یا وضعیت ماشین‌آلات را ذخیره کنند.

### ثبت رمزنگاری‌شدهٔ داده در بلاکچین

در یک طراحی آینده، ممکن است بعضی رویدادهای دستگاه یا hash داده‌ها در یک سیستم مبتنی بر بلاکچین ثبت شوند. هدف، **قابل‌تشخیص‌کردن تغییر داده و ایجاد audit trail** است؛ یعنی اگر دادهٔ ثبت‌شده بعداً تغییر کند، امکان تشخیص آن وجود داشته باشد.

قبل از اضافه‌کردن بلاکچین باید دقیق مشخص شود:

- کدام داده واقعاً ارزش ثبت‌شدن دارد؛
- آیا دادهٔ خام در دیتابیس عادی بماند و فقط hash رمزنگاری‌شدهٔ آن ثبت شود؛
- کلیدهای رمزنگاری چگونه ساخته، محافظت و بازیابی می‌شوند؛
- چه کسی اجازهٔ خواندن یا نوشتن دارد؛
- هزینه، سرعت، حریم خصوصی و نیازهای قانونی چه هستند.

برای بیشتر سیستم‌های سنسوری، ذخیرهٔ تمام دادهٔ خام روی بلاکچین معمولاً لازم نیست. روش کاربردی‌تر این است که دادهٔ عملیاتیِ رمزنگاری‌شده در دیتابیس پروژه یا فضای ذخیره‌سازی امن بماند و فقط زمانی که نیاز به اثبات مستقلِ صحت داده وجود دارد، hash یا یک رکورد audit انتخاب‌شده در بلاکچین ثبت شود.

### ماژول‌های احتمالی دیگر

- گروه‌بندی دستگاه‌ها، اتاق‌ها و موقعیت‌ها.
- نمودار و تاریخچهٔ دادهٔ سنسورها.
- هشدار برای آفلاین‌شدن دستگاه یا مقادیر خطرناک سنسورها.
- نقش‌های کاربری مثل administrator، operator و viewer.
- ابزار backup و restore.
- دسترسی از راه دور با VPN امن.
- audit log برای فرمان‌های دستگاه و تغییر تنظیمات.

## ۱۰. جمع‌بندی

MQTT GUI نقطهٔ شروع یک gateway مرکزی و محلی برای IoT است. این پروژه دستگاه‌ها، سنسورها، MQTT Broker و پنل کاربر را در یک محل جمع می‌کند. نسخهٔ فعلی روی دسترسی امن، تنظیم Broker و مانیتورینگ پایهٔ وضعیت دستگاه‌ها تمرکز دارد. نسخه‌های بعدی می‌توانند آن را به یک پلتفرم کامل‌تر برای مانیتورینگ، اتوماسیون، تاریخچهٔ داده و بررسی صحت داده تبدیل کنند.

</div>
